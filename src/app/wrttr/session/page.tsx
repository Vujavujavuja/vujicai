import { BlurSession } from '@/components/wrttr/blur-session';

export const metadata = {
  title: 'session · wrttr',
  robots: { index: false, follow: false },
};

export default function SessionPage() {
  return <BlurSession />;
}
