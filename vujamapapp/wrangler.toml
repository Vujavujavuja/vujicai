name = "vujamapapp"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
pages_build_output_dir = "."

# Environment variables will be set via Cloudflare Dashboard or wrangler CLI
# Required: OPENROUTER_API_KEY, OPENROUTER_MODEL

[env.production]
# Production environment configuration
# Custom domain: map.vujic.ai (configured in Cloudflare Dashboard)

[env.preview]
# Preview deployments (for PRs and non-main branches)
